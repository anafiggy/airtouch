# AirTouch 品牌官网 V13

## 本次更新

- 重新建立全站字号层级，正文提升至 15–17px，重要辅助信息提升至 13–14px。
- 删除首屏右侧图片上方的光环动画。
- 重构手机端布局，消除横向溢出，并将场景、案例 Tab 改为 2×2 排列。
- 调整标题换行规则，避免在语义未完整时强制换行。
- 表单提交成功后显示居中确认弹窗，失败时显示醒目的表单内错误提示。
- 页头与页脚换用 AirTouch 透明底品牌 Logo，页脚自动反白。
- 所有网页图片已嵌入 `index.html`，无需单独上传图片目录。

## EdgeOne 部署

仓库根目录请至少保留：

```text
index.html
edge-functions/
  api/
    lead.js
```

`index.html` 为单文件前端，大小约 11 MB，低于 GitHub 网页上传 25 MB 的单文件限制。`lead.js` 负责把表单线索写入飞书多维表格并发送群通知，请继续保留现有 EdgeOne 环境变量。
